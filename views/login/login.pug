extends ../layout

block content
  div.container
    div.card.my-3
      div.card-body
        h4.card-title.text-center.mb-4.mt-1.text-info ログイン
        hr
        p.text-success.text-center ログイン情報を入力してください
        if(errors)
          each error in errors
            span.text-danger.col-sm-3 ※#{error}
            br
        form(method="post" action="login")
          div.form-group
            div.input-group
              div.input-group-prepend
                span.input-group-text
                  i.fa.fa-user
              input.form-control(type="email" name="mail" placeholder="メールアドレス" value=Data['mail'] autocomplete="off")
          div.form-group
            div.input-group
              div.input-group-prepend
                span.input-group-text
                  i.fa.fa-lock
              input.form-control(type="password" name="password" placeholder="パスワード" value=Data['password'])
          div.form-group.text-center
            button.btn.btn-primary(type="submit" name="mode" value="signin") ログイン
          p.text-center
            a.btn(href="#") パスワードを忘れた方はこちら
